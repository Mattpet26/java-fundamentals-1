# Methods

## Main
runs the program

## Pluralize
  Takes a string and an integer, and returns it with a sentance with proper grammar for the integer count.

## FlipNHeads
  Uses Math.random to approximate heads or tails, and then it will return the total of flips it took to get n heads in a row.

## Clock
  Prints out the current time per second

  Stretch goal, prints out the amount of times refreshed per second